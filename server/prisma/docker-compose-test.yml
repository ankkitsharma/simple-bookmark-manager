services:
  db:
    image: mysql
    container_name: testBookmarkDb
    restart: always
    environment:
      MYSQL_USER: test-user
      MYSQL_ROOT_PASSWORD: pass2345
      MYSQL_DATABASE: test
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data:
